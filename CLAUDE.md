# wreq-ruby

Ruby bindings for Wreq HTTP client (Rust).

## Before committing
- `cargo fmt` - required, CI checks formatting
- `cargo clippy` - lint check
- `bundle exec rake test` - run tests (requires network for httpbin.io)
